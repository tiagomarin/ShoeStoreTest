<table>
    <thead>
        <th>ID</th>
        <th>NAME</th>
        <th>NAME</th>
    </thead>

    <tbody>
      <% categories.each do |category| %>
        <tr>
          <td><%= category.id %></td>
          <td><%= category.name %></td>
          <td><%= category.id %></td>
        </tr>
      <% end %>
    </tbody>
</table>

