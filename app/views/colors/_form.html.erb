<%= form_with(model: color) do |form| %>
  <% if color.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(color.errors.count, "error") %> prohibited this color from being saved:</h2>

      <ul>
        <% color.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="new-color-container__form-labels">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, placeholder: "Type color name" %>
  </div>

  <div>
    <%= form.submit "Add color", id: "new-color-container__form-button" %>
  </div>
<% end %>
