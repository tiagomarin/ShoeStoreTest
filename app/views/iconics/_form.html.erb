<%= form_with(model: iconic) do |form| %>
  <% if iconic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(iconic.errors.count, "error") %> prohibited this iconic from being saved:</h2>

      <ul>
        <% iconic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.select :iconic, @products.map {|product| [product.name, product.id]} %>
  </div>

  <div>
    <%= form.submit "Update Iconic 1" %>
  </div>
<% end %>
